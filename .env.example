# SafeMate Environment Configuration Example
# 
# IMPORTANT: This is an EXAMPLE file. Do not use this directly.
# 
# For local development, use the npm scripts which automatically load the correct environment:
#   - npm run dev              # Uses .env.dev
#   - npm run dev:preprod      # Uses .env.preprod  
#   - npm run dev:production   # Uses .env.production
#
# See ENVIRONMENT_SETUP.md for detailed instructions.
#
# =============================================================================
# AWS Cognito Configuration
# =============================================================================

# Cognito User Pool ID (get from AWS Console or Terraform output)
VITE_COGNITO_USER_POOL_ID=ap-southeast-2_XXXXXXXXX

# Cognito App Client ID (get from AWS Console or Terraform output)
VITE_COGNITO_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# Cognito Domain (get from AWS Console or Terraform output)
VITE_COGNITO_DOMAIN=your-cognito-domain

# AWS Region
VITE_COGNITO_REGION=ap-southeast-2

# =============================================================================
# API Endpoints
# =============================================================================

# API Gateway endpoints (get from Terraform output)
VITE_VAULT_API_URL=https://XXXXXXXXXX.execute-api.ap-southeast-2.amazonaws.com/dev
VITE_WALLET_API_URL=https://XXXXXXXXXX.execute-api.ap-southeast-2.amazonaws.com/dev
VITE_HEDERA_API_URL=https://XXXXXXXXXX.execute-api.ap-southeast-2.amazonaws.com/dev
VITE_ONBOARDING_API_URL=https://XXXXXXXXXX.execute-api.ap-southeast-2.amazonaws.com/dev
VITE_GROUP_API_URL=https://XXXXXXXXXX.execute-api.ap-southeast-2.amazonaws.com/dev

# =============================================================================
# Hedera Configuration
# =============================================================================

# Hedera Network (testnet or mainnet)
VITE_HEDERA_NETWORK=testnet

# MATE Token ID (get from Hedera network)
VITE_MATE_TOKEN_ID=0.0.7779374

# =============================================================================
# Application Configuration
# =============================================================================

# Application URL
VITE_APP_URL=http://localhost:5173

# Demo Mode (true for testing without real blockchain)
VITE_DEMO_MODE=false

# Debug Mode (true for development)
VITE_DEBUG_MODE=true

# =============================================================================
# File Storage Limits
# =============================================================================

# Maximum file size for Hedera storage (in bytes)
VITE_MAX_FILE_SIZE_HEDERA=52428800

# Maximum file size for direct upload (in bytes)
VITE_MAX_FILE_SIZE_DIRECT=104857600

# API Gateway file size limit (in bytes)
VITE_API_GATEWAY_LIMIT=10485760

# IPFS threshold (files larger than this use IPFS)
VITE_IPFS_THRESHOLD=52428800

# =============================================================================
# Notes
# =============================================================================
#
# 1. Replace XXXXXXXXX with actual values from your AWS/Terraform setup
# 2. Use different values for different environments (dev/preprod/prod)
# 3. Never commit real credentials to version control
# 4. Use .env.local for local-only overrides (gitignored)
# 5. See ENVIRONMENT_SETUP.md for proper environment management